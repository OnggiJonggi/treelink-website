메인 페이지(get, /)
  업체 조회(get, /company/list)
  비 로그인
    로그인(get, /member/login)
    회원가입(get, /member/join)
  로그인
    내 정보(get, /member/detail)
    로그아웃(post, /member/logout)
  로그인-권한 ADMIN
    관리자 페이지(get, /admin/main)
  로그인-권한 EVALUATOR
    평가자 페이지(get, /evaluator/main)

회원가입 페이지(/member/join)
  아이디 중복확인(get, /api/member/check-id)
  닉네임 중복확인(get, /api/member/check-nickname)
  폼 제출(post, /member/join)

로그인 페이지(/member/login)
  폼 제출(post, /member/login)

관리자 페이지(/admin/main)
  업체 추가(get, /admin/company/registor)
  평가 추가(get, /evaluator/evaluation/registor)
  평가 검색(get, /admin/evaluation/list)
  회원 검색(get, /admin/member/list)

업체 추가(/admin/company/registor)
  사업자등록번호 확인(get, /api/admin/company/check-businessno)
  폼 제출(post, /admin/company/registor)

업체 세부 뷰(/company/view/{companyNo})
  위치 보기(get, /company/{companyNo}/location)
  권한 - ADMIN
    업체 수정 페이지(get, /admin/company/{companyNo}/update)
    업체 

업체 수정(/admin/company/{companyNo}/update)
  업체 수정
  업체 삭제